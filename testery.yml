test_selection:
  suites:
    - default:
      rules:
        - rule1:
          when:
            - src_changes:
                - src/*
          then:
            - exclude_tags:
                - core   
